<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - FitLife Spor Merkezi</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <style>
        :root {
            --primary-color: #00d26a;
            --primary-dark: #00b359;
            --primary-light: #4dff91;
            --secondary-color: #1a1a2e;
            --dark-color: #0f0f1a;
            --accent-color: #00ff88;
            --text-light: #b8b8b8;
            --gradient-primary: linear-gradient(135deg, #00d26a 0%, #00ff88 100%);
            --gradient-dark: linear-gradient(135deg, #1a1a2e 0%, #0f0f1a 100%);
            --gradient-hero: linear-gradient(135deg, rgba(0,210,106,0.1) 0%, rgba(0,255,136,0.05) 100%);
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #0a0a14;
            color: #ffffff;
            overflow-x: hidden;
        }
        
        /* Page Content - Navbar için padding */
        .page-content {
            padding-top: 80px;
        }
        
        /* Homepage ve page-header olan sayfalar için padding kaldır */
        .page-content.no-padding {
            padding-top: 0;
        }
        
        /* Navbar Styles */
        .navbar {
            background: rgba(15, 15, 26, 0.95) !important;
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(0, 210, 106, 0.1);
            padding: 15px 0;
            transition: all 0.3s ease;
        }
        
        .navbar.scrolled {
            padding: 10px 0;
            background: rgba(15, 15, 26, 0.98) !important;
        }
        
        .navbar-brand {
            font-weight: 800;
            font-size: 1.8rem;
            background: var(--gradient-primary);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .navbar-brand i {
            color: var(--primary-color);
            -webkit-text-fill-color: var(--primary-color);
        }
        
        .nav-link {
            color: #ffffff !important;
            font-weight: 500;
            margin: 0 1px;
            padding: 8px 10px !important;
            border-radius: 8px;
            transition: all 0.3s ease;
            position: relative;
            font-size: 0.85rem;
        }
        
        .nav-link::after {
            content: '';
            position: absolute;
            bottom: 5px;
            left: 50%;
            width: 0;
            height: 2px;
            background: var(--gradient-primary);
            transition: all 0.3s ease;
            transform: translateX(-50%);
        }
        
        .nav-link:hover::after {
            width: 60%;
        }
        
        .nav-link:hover {
            color: var(--primary-color) !important;
        }
        
        .btn-primary {
            background: var(--gradient-primary);
            border: none;
            padding: 12px 30px;
            border-radius: 50px;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0, 210, 106, 0.3);
        }
        
        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(0, 210, 106, 0.4);
            background: var(--gradient-primary);
        }
        
        .btn-outline-primary {
            color: var(--primary-color);
            border: 2px solid var(--primary-color);
            padding: 10px 28px;
            border-radius: 50px;
            font-weight: 600;
            transition: all 0.3s ease;
            background: transparent;
        }
        
        .btn-outline-primary:hover {
            background: var(--gradient-primary);
            border-color: var(--primary-color);
            color: #000;
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(0, 210, 106, 0.3);
        }
        
        .btn-outline-light {
            border: 2px solid rgba(255,255,255,0.3);
            border-radius: 50px;
            padding: 12px 30px;
            font-weight: 600;
            transition: all 0.3s ease;
        }
        
        .btn-outline-light:hover {
            background: rgba(255,255,255,0.1);
            border-color: #fff;
            transform: translateY(-3px);
        }
        
        .bg-primary-custom {
            background: var(--gradient-primary) !important;
        }
        
        .text-primary-custom {
            color: var(--primary-color) !important;
        }
        
        /* Card Styles */
        .card {
            background: rgba(26, 26, 46, 0.6);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(0, 210, 106, 0.1);
            border-radius: 20px;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            overflow: hidden;
        }
        
        .card:hover {
            transform: translateY(-10px);
            border-color: var(--primary-color);
            box-shadow: 0 20px 40px rgba(0, 210, 106, 0.2);
        }
        
        .card-body {
            padding: 2rem;
        }
        
        /* Footer */
        footer {
            background: var(--gradient-dark);
            border-top: 1px solid rgba(0, 210, 106, 0.1);
        }
        
        /* Hero Section Base */
        .hero-section {
            min-height: 100vh;
            background: var(--dark-color);
            position: relative;
            overflow: hidden;
        }
        
        /* Dropdown */
        .dropdown-menu {
            background: rgba(26, 26, 46, 0.95);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(0, 210, 106, 0.1);
            border-radius: 15px;
            padding: 10px;
        }
        
        .dropdown-item {
            color: #fff;
            border-radius: 8px;
            padding: 10px 15px;
            transition: all 0.3s ease;
        }
        
        .dropdown-item:hover {
            background: rgba(0, 210, 106, 0.1);
            color: var(--primary-color);
        }
        
        .dropdown-divider {
            border-color: rgba(0, 210, 106, 0.1);
        }
        
        /* Feature Icon */
        .feature-icon {
            width: 80px;
            height: 80px;
            background: var(--gradient-primary);
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            transition: all 0.3s ease;
        }
        
        .feature-icon i {
            font-size: 2rem;
            color: #000;
        }
        
        .card:hover .feature-icon {
            transform: scale(1.1) rotate(5deg);
        }
        
        /* Trainer Card */
        .trainer-card img {
            height: 280px;
            object-fit: cover;
            transition: all 0.5s ease;
        }
        
        .trainer-card:hover img {
            transform: scale(1.1);
        }
        
        /* Glow Effect */
        .glow-text {
            text-shadow: 0 0 40px rgba(0, 210, 106, 0.5);
        }
        
        /* Scrollbar */
        ::-webkit-scrollbar {
            width: 10px;
        }
        
        ::-webkit-scrollbar-track {
            background: var(--dark-color);
        }
        
        ::-webkit-scrollbar-thumb {
            background: var(--primary-color);
            border-radius: 5px;
        }
        
        /* Alert Styles */
        .alert {
            border-radius: 15px;
            border: none;
        }
        
        .alert-success {
            background: rgba(0, 210, 106, 0.1);
            color: var(--primary-color);
            border: 1px solid rgba(0, 210, 106, 0.3);
        }
        
        .alert-danger {
            background: rgba(255, 71, 87, 0.1);
            color: #ff4757;
            border: 1px solid rgba(255, 71, 87, 0.3);
        }
        
        /* Pulse Animation */
        @@keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }
        
        .pulse-animation {
            animation: pulse 2s infinite;
        }
        
        /* Float Animation */
        @@keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }
        
        .float-animation {
            animation: float 3s ease-in-out infinite;
        }
        
        /* Glow Animation */
        @@keyframes glow {
            0%, 100% { box-shadow: 0 0 20px rgba(0, 210, 106, 0.3); }
            50% { box-shadow: 0 0 40px rgba(0, 210, 106, 0.6); }
        }
        
        .glow-animation {
            animation: glow 2s ease-in-out infinite;
        }

        /* Social Icons */
        .social-icon {
            width: 38px;
            height: 38px;
            border-radius: 50%;
            border: 1px solid rgba(0, 210, 106, 0.4);
            color: #00d26a;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.9rem;
            transition: all 0.3s ease;
            text-decoration: none;
        }

        .social-icon:hover {
            background: linear-gradient(135deg, #00d26a 0%, #00ff88 100%);
            color: #000;
            border-color: transparent;
            transform: translateY(-3px);
        }
    </style>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
            <div class="container">
                <a class="navbar-brand" asp-controller="Home" asp-action="Index">
                    <i class="bi bi-lightning-charge-fill"></i> FitLife
                </a>
                <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav me-auto ms-3">
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Home" asp-action="Index">
                                <i class="bi bi-house me-1"></i> Ana Sayfa
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Service" asp-action="Index">
                                <i class="bi bi-grid me-1"></i> Hizmetler
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Trainer" asp-action="Index">
                                <i class="bi bi-people me-1"></i> Antrenörler
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Appointment" asp-action="Index">
                                <i class="bi bi-calendar-check me-1"></i> Randevu Al
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="AI" asp-action="Index">
                                <i class="bi bi-robot me-1"></i> AI Öneri
                            </a>
                        </li>
                        @if (User.Identity?.IsAuthenticated == true && User.IsInRole("Admin"))
                        {
                            <li class="nav-item">
                                <a class="nav-link" asp-controller="Admin" asp-action="Index" style="color: var(--primary-color) !important;">
                                    <i class="bi bi-gear me-1"></i> Admin Panel
                                </a>
                            </li>
                        }
                    </ul>
                    <ul class="navbar-nav">
                        @if (User.Identity?.IsAuthenticated == true)
                        {
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
                                    <i class="bi bi-person-circle me-1"></i> @User.Identity?.Name
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end">
                                    <li>
                                        <a class="dropdown-item" asp-controller="Account" asp-action="Profile">
                                            <i class="bi bi-person me-1"></i> Profilim
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" asp-controller="Account" asp-action="MyAppointments">
                                            <i class="bi bi-calendar3 me-1"></i> Randevularım
                                        </a>
                                    </li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li>
                                        <form asp-controller="Account" asp-action="Logout" method="post">
                                            <button type="submit" class="dropdown-item" style="color: #ff4757;">
                                                <i class="bi bi-box-arrow-right me-1"></i> Çıkış Yap
                                            </button>
                                        </form>
                                    </li>
                                </ul>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="nav-link" asp-controller="Account" asp-action="Login">
                                    <i class="bi bi-box-arrow-in-right me-1"></i> Giriş Yap
                                </a>
                            </li>
                            <li class="nav-item ms-2">
                                <a class="btn btn-primary" asp-controller="Account" asp-action="Register">
                                    <i class="bi bi-person-plus me-1"></i> Üye Ol
                                </a>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <main>
        @if (TempData["Success"] != null)
        {
            <div class="container" style="margin-top: 100px;">
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <i class="bi bi-check-circle"></i> @TempData["Success"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            </div>
        }
        @if (TempData["Error"] != null)
        {
            <div class="container" style="margin-top: 100px;">
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <i class="bi bi-exclamation-circle"></i> @TempData["Error"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            </div>
        }
        <div class="page-content @(ViewData["NoTopPadding"] != null ? "no-padding" : "")">
            @RenderBody()
        </div>
    </main>

    <footer class="text-white py-5 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-4 mb-4" data-aos="fade-up">
                    <h5 class="mb-4"><i class="bi bi-lightning-charge-fill text-primary-custom"></i> <span class="text-primary-custom">FitLife</span> Spor Merkezi</h5>
                    <p class="text-white-50">Modern ekipmanları ve uzman kadrosuyla sağlıklı yaşam yolculuğunuzda yanınızdayız.</p>
                    <div class="d-flex gap-2 mt-4">
                        <a href="#" class="social-icon">
                            <i class="bi bi-facebook"></i>
                        </a>
                        <a href="#" class="social-icon">
                            <i class="bi bi-instagram"></i>
                        </a>
                        <a href="#" class="social-icon">
                            <i class="bi bi-twitter-x"></i>
                        </a>
                        <a href="#" class="social-icon">
                            <i class="bi bi-youtube"></i>
                        </a>
                    </div>
                </div>
                <div class="col-md-2 mb-4" data-aos="fade-up" data-aos-delay="100">
                    <h6 class="text-primary-custom mb-4">Hızlı Linkler</h6>
                    <ul class="list-unstyled">
                        <li class="mb-2"><a href="#" class="text-white-50 text-decoration-none hover-primary">Ana Sayfa</a></li>
                        <li class="mb-2"><a href="#" class="text-white-50 text-decoration-none hover-primary">Hizmetler</a></li>
                        <li class="mb-2"><a href="#" class="text-white-50 text-decoration-none hover-primary">Antrenörler</a></li>
                        <li class="mb-2"><a href="#" class="text-white-50 text-decoration-none hover-primary">İletişim</a></li>
                    </ul>
                </div>
                <div class="col-md-3 mb-4" data-aos="fade-up" data-aos-delay="200">
                    <h6 class="text-primary-custom mb-4">Çalışma Saatleri</h6>
                    <ul class="list-unstyled text-white-50">
                        <li class="mb-2"><i class="bi bi-clock text-primary-custom me-2"></i> Pazartesi - Cuma: 06:00 - 23:00</li>
                        <li class="mb-2"><i class="bi bi-clock text-primary-custom me-2"></i> Cumartesi: 09:00 - 18:00</li>
                        <li class="mb-2"><i class="bi bi-clock text-primary-custom me-2"></i> Pazar: Kapalı</li>
                    </ul>
                </div>
                <div class="col-md-3 mb-4" data-aos="fade-up" data-aos-delay="300">
                    <h6 class="text-primary-custom mb-4">İletişim</h6>
                    <ul class="list-unstyled text-white-50">
                        <li class="mb-2"><i class="bi bi-geo-alt text-primary-custom me-2"></i> Sakarya Üniversitesi</li>
                        <li class="mb-2"><i class="bi bi-telephone text-primary-custom me-2"></i> 0264 295 00 00</li>
                        <li class="mb-2"><i class="bi bi-envelope text-primary-custom me-2"></i> info@fitlife.com</li>
                    </ul>
                </div>
            </div>
            <hr class="border-secondary my-4">
            <div class="row align-items-center text-center">
                    <p class="text-white-50 mb-0">&copy; 2025 <span class="text-primary-custom">FitLife</span> Spor Merkezi. Tüm hakları saklıdır.</p>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <script>
        // Initialize AOS
        AOS.init({
            duration: 800,
            easing: 'ease-in-out',
            once: true
        });
        
        // Navbar scroll effect
        window.addEventListener('scroll', function() {
            const navbar = document.querySelector('.navbar');
            if (window.scrollY > 50) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });
    </script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
